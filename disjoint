class Disjoint:
    def __init__(self,V):
        self.rank = [0]*V
        self.parent = [i for i in range(0,V)]
    def findUParent(self,node):
        if(node == self.parent[node]):
            return node
        self.parent[node] = self.findUParent(self.parent[node])
        return self.parent[node]
    def findParent(self,u,v):
        if(self.findUParent(u) == self.findUParent(v)):
            return True
        return False
    def unionByRank(self,u,v):
        pu = self.findUParent(u)
        pv = self.findUParent(v)
        if(pu == pv):
            return
        if(self.rank[pu] == self.rank[pv]):
            self.parent[pv] = pu
            self.rank[pu]+=1
        elif(self.rank[pu]<self.rank[pv]):
            self.parent[pu] = pv
        elif(self.rank[pv]<self.rank[pu]):
            self.parent[pv] = pu
ob = Disjoint(7)
ob.unionByRank(0,1)
ob.unionByRank(1,2)
if(ob.findParent(1,3)):
    print(True)
else:
    print(False)
